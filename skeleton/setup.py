import setuptools

setuptools.setup(
    # TODO: Fill in.
    # Note, for this src-based layout, use:
    # package_dir={
    #     "": "src"
    # },
    # packages=setuptools.find_packages("src"),
)
